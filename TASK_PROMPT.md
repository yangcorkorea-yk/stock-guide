# Claude Code 실행 프롬프트 — Phase 4

먼저 `CLAUDE.md`와 `ROADMAP.md`를 읽고 기존 코드(app.py, analysis.py, auto_trader.py)의 구조·원칙을 파악해줘. 그 원칙(예측 아님 / 서술형 라벨 / 모바일 우선 / 데이터 한계 표기 / 키는 secrets)을 **절대 깨지 말고** 아래 4가지를 구현해줘. 각 작업 후 `python -m streamlit run app.py`로 로컬 동작을 확인하고, 변경은 작은 커밋으로 나눠줘.

## 작업 1 — 테마·섹터·종목 확장
- `analysis.py`의 `SECTORS`를 **20개 이상**의 섹터로, 각 섹터에 **10개 이상**의 대표 미국 티커로 확장.
- `THEMES`를 **12개 이상**의 테마로 확장. 각 테마는 기존 구조 유지: `{desc(한국어, 초보자용), chain: [{name, stocks:[...]}, ...]}`. 테마당 종목 총 10개 이상이 되도록 단계(chain)별로 배분.
- ⚠️ **모든 티커는 실재하는 미국 상장 티커여야 함.** 추가 후 스크립트로 각 티커를 Alpaca(또는 yfinance)로 조회해 유효성 검증하고, 응답 없는 티커는 교체할 것. 검증 결과를 출력으로 보여줘.
- 설명문은 한국어, 초보자 눈높이. 라벨/구조는 기존과 동일하게.

## 작업 2 — "🔥 뜨는 테마" 탭 (뉴스 언급량 자동 순위)
- 새 탭 추가: 최근(예: 최근 3일) 뉴스에서 각 테마의 키워드 언급량을 세어 **순위**로 보여줌. 항목: 순위·테마명·언급 건수·간단 설명, 누르면 기존 테마 상세로 연결.
- **자동화 구조(권장)**: Streamlit Cloud엔 스케줄러가 없으므로 **GitHub Actions 일일 cron**으로 처리.
  - `scripts/rank_themes.py`: 뉴스 소스에서 헤드라인 수집 → 각 THEME의 키워드 매칭 건수 집계 → `data/hot_themes.json`(테마명·count·갱신시각) 저장.
  - `.github/workflows/daily.yml`: 매일 1회 위 스크립트 실행 후 `data/hot_themes.json`을 커밋(자동 재배포 유발).
  - 앱은 `data/hot_themes.json`을 읽어 표시(파일 없으면 "집계 전"으로 graceful).
- **뉴스 소스**: 기존 Alpaca 키로 쓸 수 있는 **Alpaca News API**(`/v1beta1/news`)를 우선 검토(추가 키 불필요). 여의치 않으면 별도 뉴스 API 키를 secrets로 받아 사용 — 어떤 키가 있는지는 나(사용자)에게 물어봐줘.

## 작업 3 — 뉴스 요약 + 원문 링크 (종목 상세)
- `show_detail`의 "💡 왜 이 종목?"/뉴스 부분을 업그레이드: 선택한 **종목 + 그 종목의 섹터** 관련 최근 뉴스를 모아
  1) **LLM으로 한국어 3~4줄 요약**, 2) **원문 링크 목록**(제목+출처+링크)으로 표시.
- LLM은 환경변수/secrets의 API 키로 호출(비용 작은 모델 권장, 예: Claude Haiku). 키 이름·모델은 설정 가능하게.
- LLM/뉴스 호출은 `st.cache_data`로 캐시(예: 1시간)해 호출·비용 절감. 실패 시 요약 생략하고 링크만, 그것도 없으면 기존처럼 graceful.
- 요약 말미에 "AI 요약, 원문 확인 권장" 같은 짧은 주의.

## 작업 4 — 진입/매도/손절 참고 가격대 (개인용)
- 종목 상세에 **규칙 기반 참고 가격대**를 추가:
  - **진입 참고**: 예) 볼린저 하단/최근 지지선/이동평균 부근.
  - **매도 참고**: 예) 볼린저 상단/최근 고점(저항) 부근.
  - **손절 라인**: 예) 최근 스윙 저점, 또는 진입가 − ATR×배수.
- 각 값이 **어떤 규칙으로 나왔는지** 함께 표기하고, 차트에 수평선으로도 표시.
- ⚠️ **반드시 "예측/추천이 아니라 규칙으로 계산한 참고값"으로 명시**하고, "이런 기계적 규칙은 과거 검증상 단순보유를 못 이겼다"는 한계 주의를 같이 표기. 서술형·비조언 톤 유지. (자동 주문/매매 기능은 만들지 말 것 — 표시만.)

## 공통 / 마무리
- 새 라이브러리는 `requirements.txt`에 추가. 새 키는 `.gitignore`/secrets 규칙 준수, 코드에 하드코딩 금지.
- 모바일에서 표·차트가 안 깨지는지 확인(기존 `st.dataframe`/`st.pills` 패턴 유지).
- 작업별로 무엇을 바꿨는지 요약해줘. 불명확한 점(뉴스 API 종류, LLM 키 등)은 진행 전에 나에게 질문해줘.
